{
  "name": "@kortex/db",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "bun run seed.ts",
    "db:reset": "prisma db push --force-reset && bun run seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.21.0",
    "prisma": "^7.9.1"
  },
  "peerDependencies": {
    "typescript": "^5 || ^6"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "@types/dotenv": "^8.2.3",
    "dotenv": "^17.4.2",
    "pg": "^8.16.3"
  }
}
