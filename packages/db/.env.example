# packages/db — copy to packages/db/.env and fill in.
# Only needed for running Prisma CLI commands directly from this package
# (e.g. `bunx prisma studio`); apps/web reads its own copy of this value
# from apps/web/.env.local.

DATABASE_URL=postgresql://kortex:kortex_dev_password@localhost:5433/kortex_dev
